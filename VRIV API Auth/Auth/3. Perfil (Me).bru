meta {
  name: 3. Perfil (Me)
  type: http
  seq: 3
}

post {
  url: {{vriv}}/auth/me
  body: none
  auth: inherit
}

headers {
  Accept: application/json
  Authorization: Bearer {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
